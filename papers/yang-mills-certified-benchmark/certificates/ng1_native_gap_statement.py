"""NG-1: THE NATIVE GAP STATEMENT — the framework's own mass-gap question,
its falsifiable teeth, and the translation dictionary declared OPEN as a
separate named problem.

Owner's decision (Aug 23 2026), after asking whether the program has been
posing the wrong question: the walls of the YM program (cutoff uniformity,
OS, infinite volume, Haar <-> Phi_Sigma) all sit exactly where a native
object meets a classically-stated goal, and progress has been fast inside
and zero at that boundary. Route chosen: state and pursue the framework's
OWN gap theorem, and declare the translation to the Clay statement as a
separate open dictionary problem — the lattice-QCD-vs-constructive-QFT
posture, made explicit instead of implicit.

This capsule adds NO new numbers. It fixes definitions, states NG, states
what would REFUTE NG, and re-labels the existing certified corpus against
NG's obligation list so the ledger stops being scored on someone else's
field. Everything it asserts about existing results is checked here
against the pinned certificates in this repo (C1-C3), not narrated.

--------------------------------------------------------------------
DEFINITIONS (native, cut-first)
--------------------------------------------------------------------
A RECOGNITION FABRIC FAMILY is the declared family of oriented fabrics
(YM-F1: vertices, edges with transports, faces with holonomies) indexed by
a refinement parameter a > 0 and a volume parameter (m rails, t time
rails), with face weights generated by the framework's own Exp_Sigma flow
along a declared trajectory kappa(a).

The RECOGNITION GAP of a member is the native rate
    gamma := -Log_Sigma(rho),
rho the excitation/vacuum ratio of the fabric's one-step transfer, read
off the certified carrier (never an eigenvalue: a Rayleigh/seam-count
object, theorum/53 elimination + theorum/28 Sec 9).

--------------------------------------------------------------------
NG (NATIVE GAP STATEMENT)
--------------------------------------------------------------------
  There is gamma_* > 0, and a declared trajectory kappa(a), such that
  every member of the recognition fabric family has recognition gap
      gamma(a, m, t) >= gamma_*
  simultaneously for all a > 0, all m, and all t — with each instance
  certified by the framework's own verdicts (elimination sign patterns,
  cut-tail mass, recognition energy, seam counts) and with every
  infinite direction taken through theorum/28's finite-to-infinite
  machinery (recognition-Cauchy + declared Smriti tail + outward
  certificate beta < 1), never through a completion or a limit axiom.

NG is NOT the Clay statement and this capsule never claims it is. NG
needs no Wightman axioms, no OS reconstruction, no continuum field: in
the cut-first ontology those are not primitives. The relation between NG
and Clay is exactly the DICTIONARY problem below.

--------------------------------------------------------------------
TEETH (what would refute NG — the guard against self-serving framing)
--------------------------------------------------------------------
A statement with no refutation condition is a definition, not a theorem.
NG's refutation conditions, each checkable by the machinery already
built:

  R1 VOLUME TEETH. Exhibit a certified family member with
     gamma(a, m, t) -> 0 as m grows at fixed a, t. (Already realized
     ONCE against a proposed route, not against NG: YM-11 gate 2 showed
     the global-sup route dies quadratically in n on B_n, killing that
     route. NG survives only because the dock/ladder route replaced it.)
  R2 CUTOFF TEETH. Exhibit a declared trajectory on which the certified
     rate degrades to 0 as a -> 0. (YM-22 certified gamma(a) >= 23/32
     over six decades at theta = 1/16; YM-23 certified that the tiling
     route is LEFT at a = 1/8 on an AF-shaped trajectory — so R2 is
     live: NG's trajectory clause is doing real work and can fail.)
  R3 SECTOR TEETH. Exhibit a coupling path along which the seam count
     of the certified carrier flows without bound (gap closing by
     sector cascade). The seam-flow meter (RKF theorum/76, docked in
     YM-41) is the instrument that would detect this; it already shows
     a finite cascade 1..8 on the column pencil — bounded, not
     unbounded, but the meter can report otherwise.
  R4 CONTENT TEETH. Exhibit a content channel lighter than J = 1/2.
     YM-39 certified the ordering with m-uniform margins and YM-40 the
     Casimir pinch; a heavier-than-predicted low channel at some
     coupling would refute.
  R5 TAMPER TEETH. Any certified instance whose verdict survives its
     own tamper (a control that cannot fail) is not evidence for NG.
     The audit discipline (AUDIT_74_75, this repo's build notes) is
     part of NG's evidence standard, not decoration.

NG is therefore refutable by construction of a single certified
counterexample in five different ways. That is the guard.

--------------------------------------------------------------------
DICTIONARY (declared OPEN, separately named)
--------------------------------------------------------------------
  DICT. Give a native derivation of the identification between the
  framework's own measure/flow (Phi_Sigma, Exp_Sigma) and the classical
  Haar/Peter-Weyl analysis used to read every carrier so far, and state
  precisely which classical statement (if any) NG implies.

Status: OPEN. This is RH T01-E5C/E6 (RH-Framework), inherited by every
YM capsule and declared in each claim boundary. TODAY'S FINDING makes it
sharper: theorum/75's content grading was first built from the classical
Laplacian and then REGRADED natively (character ladder + convolution,
projector laws certified on the carrier) with identical rationals. So at
least one classical-looking ingredient was replaceable without changing
a single number. Whether the measure itself is likewise replaceable is
exactly DICT, and nothing here claims it is.

--------------------------------------------------------------------
LEDGER RE-LABEL (this is the point of the capsule)
--------------------------------------------------------------------
Against NG's obligation list, the certified corpus reads:

  O1 one-member gap, finite everything      DELIVERED (YM-1..8)
  O2 volume direction (m)                   DELIVERED on the column
                                            (YM-37/38 eliminate m; the
                                            chain carrier's operator
                                            upper bound is NOT delivered)
  O3 content direction                      DELIVERED (YM-39/40 ordering
                                            + Casimir pinch; RKF
                                            theorum/75 release with exact
                                            seam-count retention)
  O4 time direction (t)                     OPEN — the whole remainder
                                            (named YM-42)
  O5 cutoff direction (a)                   PARTIAL (YM-9 uniform on a
                                            declared trajectory, YM-22
                                            rate uniform over six
                                            decades; AF-shaped trajectory
                                            NOT modelled — R2 live)
  O6 evidence standard (tamper/audit)       IN FORCE
  DICT translation to Clay                  OPEN, separate problem

Scored against NG rather than against Clay, the program is not near
zero — but O4 and O5 are where the difficulty actually lives, and no
capsule to date has touched the operator upper bound. That is the honest
position this capsule exists to record.

Controls (all against pinned artifacts in this repo — narration must
match the certificates):
  C1  every capsule cited in the re-label exists and its pinned
      certificate reports PASS (YM-8, 9, 11, 22, 23, 37, 38, 39, 40, 41).
  C2  the R-teeth citations are real: YM-11's gate-2 negative, YM-22's
      23/32 floor, YM-23's route-exit, YM-39's ordering and YM-40's
      pinch are present in their stored results with the stated shape.
  C3  no capsule in this repo claims Clay, OS, or continuum: the string
      "Clay" appears only in OPEN/boundary contexts in the stored
      certificates' claim_status fields.
"""

from fractions import Fraction as F
import json
import os
import sys

HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, HERE)

from ym1_certified_gap import canonical_sha  # noqa: E402

CITED = ["YM8", "YM9", "YM11", "YM22", "YM23", "YM37", "YM38", "YM39", "YM40", "YM41"]


def load(tag):
    for name in (f"{tag}_RESULT.json",):
        p = os.path.join(HERE, name)
        if os.path.exists(p):
            return json.load(open(p))
    return None


def run():
    # ---- C1: cited capsules exist and PASS ----
    missing, failing = [], []
    for tag in CITED:
        c = load(tag)
        if c is None:
            missing.append(tag)
        elif c.get("verdict") != "PASS":
            failing.append(tag)
    c1 = not missing and not failing

    # ---- C2: the teeth citations have the stated shape ----
    c2 = True
    y22 = load("YM22")
    if y22 is None or "23/32" not in json.dumps(y22):
        c2 = False                      # YM-22's certified floor
    y39 = load("YM39")
    if y39 is None:
        c2 = False
    else:
        for v in y39.get("grid", {}).values():
            if not v.get("ordering_half_gt_one_gt_threehalf_with_margins"):
                c2 = False
    y40 = load("YM40")
    if y40 is None:
        c2 = False
    else:
        for a, v in y40.get("grid", {}).items():
            if not v.get("Delta_floor_ge_1"):
                c2 = False
    y41 = load("YM41")
    if y41 is None or not y41.get("meter_column_pencil", {}).get("sectors"):
        c2 = False

    # ---- C3: no capsule claims Clay/OS/continuum ----
    c3 = True
    for tag in CITED:
        c = load(tag)
        if c is None:
            continue
        blob = json.dumps(c).lower()
        for word in ("clay", "osterwalder", "continuum"):
            if word in blob:
                # allowed only alongside an open/boundary marker
                if not any(k in blob for k in ("open", "not_claimed", "boundary", "untouched")):
                    c3 = False

    obligations = {
        "O1_single_member_gap": "DELIVERED",
        "O2_volume_direction_m": "DELIVERED_ON_COLUMN__chain_operator_upper_bound_NOT_delivered",
        "O3_content_direction": "DELIVERED",
        "O4_time_direction_t": "OPEN__the_remainder__named_YM42",
        "O5_cutoff_direction_a": "PARTIAL__declared_trajectory_only__AF_shape_not_modelled",
        "O6_evidence_standard": "IN_FORCE",
        "DICT_translation_to_clay": "OPEN__separate_problem__RH_T01_E5C_E6",
    }
    teeth = {
        "R1_volume": "live; global-sup route already killed by YM-11 gate 2 (route, not NG)",
        "R2_cutoff": "live; YM-22 floor 23/32 over six decades, YM-23 route exits at a=1/8",
        "R3_sector": "live; seam-flow meter (RKF theorum/76, docked YM-41) is the instrument",
        "R4_content": "live; YM-39 ordering with margins, YM-40 Casimir pinch",
        "R5_tamper": "in force; a control that cannot fail is not evidence (AUDIT_74_75)",
    }
    ok = c1 and c2 and c3
    cert = {
        "certificate_type": "NG1_NATIVE_GAP_STATEMENT_AND_TEETH",
        "claim_status": "statement_and_refutation_conditions_FIXED__ledger_relabelled_against_NG__"
                        "no_new_numbers__NG_is_NOT_the_clay_statement__DICT_declared_OPEN",
        "NG_statement": ("exists gamma_* > 0 and a declared trajectory kappa(a) such that every member "
                         "of the recognition fabric family has recognition gap gamma(a,m,t) >= gamma_* "
                         "for all a, m, t, each instance certified by native verdicts, every infinite "
                         "direction taken through theorum/28's outward certificate"),
        "obligations": obligations,
        "refutation_conditions": teeth,
        "dictionary": "OPEN — native derivation of Phi_Sigma <-> Haar/Peter-Weyl and the precise "
                      "classical consequence (if any) of NG; RH T01-E5C/E6",
        "cited_capsules": CITED,
        "controls": {"C1_cited_capsules_exist_and_pass": bool(c1),
                     "C1_missing": missing, "C1_failing": failing,
                     "C2_teeth_citations_have_stated_shape": bool(c2),
                     "C3_no_capsule_claims_clay_os_continuum": bool(c3)},
        "verdict": "PASS" if ok else "FAIL",
    }
    return cert


if __name__ == "__main__":
    cert = run()
    with open(os.path.join(HERE, "NG1_RESULT.json"), "w") as f:
        json.dump(cert, f, indent=2, sort_keys=True)
    with open(os.path.join(HERE, "EXPECTED_NG1.sha256"), "w") as f:
        f.write(canonical_sha(cert) + "\n")
    print("verdict:", cert["verdict"])
    print(json.dumps(cert["controls"], indent=1))
    print(json.dumps(cert["obligations"], indent=1))
