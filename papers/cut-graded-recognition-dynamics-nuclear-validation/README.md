# Cut-Graded Recognition Dynamics with Nuclear, Atomic, and AI Validation

Stage-III arXiv manuscript source with three source-bounded adapters: nuclear response prediction, periodic/ground-configuration validation, and context-grounded AI hallucination control.

Compile from the `source` directory:

```bash
pdflatex main.tex
pdflatex main.tex
pdflatex main.tex
```

The compiled `main.bbl` is included. The AI section reports fixed finite benchmarks and does not claim a universal truth oracle or population-level hallucination rate.
